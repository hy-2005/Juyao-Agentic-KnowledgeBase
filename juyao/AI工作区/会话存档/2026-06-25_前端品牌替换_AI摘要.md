# 2026-06-25 前端若依品牌替换 — AI 摘要

> 前进言：了解 juyao-ui 从若依框架品牌迁移为 juyao-agentic-rag 系统时的改动范围。

## 核心结论

- 图谱页面组件（`KgFullGraphShell.vue` 等）本身无若依文案；可见品牌来自全局 `VUE_APP_TITLE` 与首页。
- 已将 `VUE_APP_TITLE` 统一为 `juyao-agentic-rag系统`，覆盖开发/测试/生产三份 `.env` 及 `vue.config.js` 默认值。
- 首页 `index.vue` 标题、介绍、技术栈、联系信息已替换为 juyao-agentic-rag 相关内容；`package.json` description/author 已更新。
- 修改 `.env` 后需重启 `npm run dev` 才能生效。

## 决策与权衡

| 决策 | 理由 |
|------|------|
| 不改 `tool/gen` 中 `com.ruoyi` 示例 | 属代码生成开发工具，非用户可见界面 |
| 联系区移除若依微信/支付宝 | 避免误导；改为项目说明文案 |

## 待验证

- 用户重启前端后侧边栏、登录页、浏览器标签是否显示新标题

## 关联

- `juyao-ui/.env.development` / `.env.production` / `.env.staging`
- `juyao-ui/src/views/index.vue`

## 待办衍生

- （无）
