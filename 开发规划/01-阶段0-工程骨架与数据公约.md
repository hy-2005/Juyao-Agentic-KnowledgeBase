                                      # 阶段 0：工程骨架与数据公约

## 执行状态

- **当前**：进行中
- **更新记录**：
  - 2026-05-06：仓库内已建立 Python RAG 子目录 `juyao-agentic-rag/`（`requirements.txt` + `src/rag_core/`）。
  - 2026-05-07：构建入口改为 `pyproject.toml`（`pip install -e .` 仍从 `requirements.txt` 拉取依赖）；移除根目录 `setup.py`、占位空目录与误置于 `src/` 的 `*.egg-info`。
  - 2026-05-07：入库链路完成稳定性修复：Qdrant 点 ID 改为基于 `chunk_id` 生成的稳定 UUID（满足 Qdrant ID 约束并保持幂等覆盖）。
  - 2026-05-07：切块模块修复 `split_into_chunks` 函数命名错误；默认切块参数调整为 `chunk_size=300`、`chunk_overlap=60`，用于规避 embedding 输入超长导致的 400 错误。
  - 2026-05-07：切分策略已落地为「段落级候选 + `qwen2:1.5b` 语义断点选择 + 前后重叠上下文」，不再按标点符号强制硬切 chunk。

**顺序**：第 1 步（可与阶段 1 部分并行，但必须先完成数据公约）

## 本阶段技术栈

| 类别 | 选型 |
|------|------|
| 语言 / 编排 | Python 3、**LangChain**（管线骨架） |
| Chunk 语义切分 | **Ollama**：`qwen2:1.5b`；重叠兜底策略见上文 |
| 向量库（最小验证） | **Qdrant（本地）**（可先最小集合验证入库链路） |
| Embedding（验证写入时） | **Ollama**：`mxbai-embed-large:latest`（与 [技术栈约定.md](./技术栈约定.md) 一致） |
| 配置与依赖 | **`requirements.txt`** + **`pyproject.toml`**（路径：`juyao-agentic-rag/`）；可编辑安装 `pip install -e .`；运行时配置当前为 `src/rag_core/config.py` 硬编码 |
| 全文索引 | 本阶段可不启用 **Elasticsearch** |

细化与待定项见 [技术栈约定.md](./技术栈约定.md)。

## 开发目标

1. 建立可重复执行的「文档 → 处理 → 可检索存储」最小流水线。
2. 约定 **chunk 全局唯一 ID**，保证后续向量库与 Neo4j 能指向**同一段文本**，避免第三方 GraphRAG 自拆 chunk 后与向量不一致。
3. **Chunk 切分（当前约定）**：采用「段落级候选 + 小模型语义断点」策略：先按段落聚合候选区间，再由 **Ollama `qwen2:1.5b`** 选择语义断点；随后追加相邻 chunk **前后重叠上下文**，减少边界信息丢失。切分模型与后续 embedding/问答模型分离配置。
4. 打通配置与模型调用（Python + LangChain 等），为后续阶段预留接口；本阶段需能拉起 Ollama 并完成一次端到端切分验证。

## 交付物

- 项目目录结构、依赖与配置说明（**`requirements.txt`** / **`pyproject.toml`** + `src/rag_core/config.py`）。
- 统一 **chunk 元数据字段**：至少包含 `chunk_id`、`source_doc_id`、`offset` 或序号、可选 `hash`；若启用重叠，记录 **`overlap` 范围或与相邻 chunk 的关联**，便于增量更新时成批失效。
- **语义切分**：基于 `qwen2:1.5b` 的切割脚本或管线说明（含 Ollama 地址、模型名、重叠参数配置项）。
- 最小数据集跑通：1～2 份清洗后的文本/PDF 经上述策略切块后入库（向量库为本地 Qdrant，默认 `http://localhost:6333`）。
- 配置分层：API Key、各模型名（切分 / embedding / 生成）、向量维度等与代码分离。

## 验收要点

- 同一批文档可重复入库，**chunk_id 稳定**（同一文本同一规则下 ID 不变）。
- 语义切分 + 重叠策略跑通，抽查相邻边界 topic 不被「标点硬切断」到不可检索（允许用重叠段补救）。
- 团队内有一份简短「数据公约」说明：谁生成 chunk_id、哪些模块必须读写该字段。

## 依赖与风险

- **依赖**：无前置阶段。
- **风险**：若此处 ID 不统一，阶段 3～4 图谱与向量会对不齐，返工成本高。
